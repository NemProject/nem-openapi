type: integer
description: >
    The network id. The following network ids are supported:
    * `104 (hex 0x68)` - The main network id.
    * `96 (hex 0x60)` - The mijin network id.
    * `-104 (hex 0x98)` - The test network id.
